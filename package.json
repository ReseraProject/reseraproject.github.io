{
  "name": "resera",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "preview": "nuxt preview",
    "generate": "bun run validate:content && nuxt generate",
    "typecheck": "nuxt typecheck",
    "validate:content": "bun run scripts/validate-opportunities.ts",
    "test": "bun test"
  },
  "type": "module",
  "dependencies": {
    "nuxt": "^4.5.2",
    "vue": "^3.5.0",
    "vue-router": "^5.2.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vue-tsc": "^3.2.0"
  }
}
